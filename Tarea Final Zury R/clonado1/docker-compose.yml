version: "3.1"
services:
  islasgeci:
    build:
      context: .
      dockerfile: Dockerfile
    image: islasgeci/seabird_tracking:latest
    command: bash
    volumes:
      - $PWD:/workdir
